# ============================================================
# AdStrategy AI — 의존성 목록
# ============================================================
# 설치: pip install -r requirements.txt
# 선택적 패키지: pip install -r requirements-optional.txt (또는 아래 주석 해제)
# ============================================================

# --- 기본 (필수) ---
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# --- ML 모델 (필수) ---
xgboost>=2.0.0
lightgbm>=4.0.0

# --- 데이터 수집 (Phase 2) ---
requests>=2.31.0          # World Bank API, 환율 API
holidays>=0.40            # 국가별 공휴일

# --- 모델 해석 ---
shap>=0.44.0              # SHAP 피처 중요도 분석

# --- 하이퍼파라미터 튜닝 ---
optuna>=3.4.0             # Optuna 자동 튜닝

# --- 시각화 ---
matplotlib>=3.7.0
seaborn>=0.12.0
plotly>=5.18.0            # 인터랙티브 차트

# --- 통계 검증 ---
scipy>=1.11.0             # ANOVA, t-test, Chi², Shapiro-Wilk 등

# --- AI 에이전트 + 웹 UI ---
openai>=1.40.0            # GPT-4o Function Calling
streamlit>=1.38.0         # Streamlit 웹 앱
python-dotenv>=1.0.0      # .env 파일 로드

# ============================================================
# 선택적 패키지 (API 키 또는 추가 기능 필요 시 주석 해제)
# ============================================================
# kaggle>=1.6.0           # Phase 1: Kaggle 데이터셋 (kaggle.json 필요)
# fredapi>=0.5.0          # Phase 2: FRED 경제지표 (FRED_API_KEY 필요)
# pytrends>=4.9.0         # Phase 2: Google Trends
# sdv>=1.8.0              # CTGAN 합성 데이터 (GPU 권장)
# imbalanced-learn>=0.11  # SMOTE 오버샘플링
# mcp>=1.0.0              # MCP 서버 (외부 LLM 연동)
