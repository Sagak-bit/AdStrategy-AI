# ============================================================
# 광고 모델 성능 향상 데이터 파이프라인 의존성
# ============================================================

# --- 기본 (필수) ---
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0

# --- 모델 (권장) ---
xgboost>=2.0.0
lightgbm>=4.0.0

# --- 데이터 수집 (Phase별) ---
requests>=2.31.0          # Phase 2-1: World Bank API, 환율 API
holidays>=0.40            # Phase 2-2: 국가별 공휴일

# --- 선택적 (API 키 필요) ---
# kaggle>=1.6.0           # Phase 1: Kaggle 데이터셋 다운로드 (kaggle.json 필요)
# fredapi>=0.5.0          # Phase 2-1: FRED 경제지표 (API 키 필요)
# pytrends>=4.9.0         # Phase 2-3: Google Trends

# --- 모델 해석 (강력 권장) ---
shap>=0.44.0              # SHAP 피처 중요도 분석

# --- 하이퍼파라미터 튜닝 (선택) ---
optuna>=3.4.0             # Optuna 자동 하이퍼파라미터 튜닝

# --- 합성 데이터 (선택) ---
# sdv>=1.8.0              # CTGAN 합성 데이터 (무거움, 필요시만)
# imbalanced-learn>=0.11  # SMOTE 오버샘플링

# --- 시각화 (기존 코드 호환) ---
matplotlib>=3.7.0
seaborn>=0.12.0

# --- AI 에이전트 (AdStrategy AI) ---
openai>=1.40.0            # GPT-4o Function Calling
streamlit>=1.38.0         # 웹 UI
python-dotenv>=1.0.0      # .env 파일 로드
plotly>=5.18.0            # 인터랙티브 차트
